# ─────────────────────────────────────────────────────────
#  Lucid AI — Agent Engine Dependencies
# ─────────────────────────────────────────────────────────

# Core Server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.0.0
websockets>=12.0
python-multipart>=0.0.6

# Authentication
python-jose[cryptography]>=3.3.0
cryptography>=41.0.0

# Database
supabase>=2.5.0

# LLM Providers
google-generativeai>=0.8.0
litellm>=1.50.0

# Docker (for Docker host management)
docker>=7.1.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.27.0
Pillow>=10.0.0

# Cloud Sandbox (preview) — v2 required for Sandbox.create() API
e2b>=2.0.0,<3.0.0

# Redis (session persistence + future job queue)
redis[hiredis]>=5.0.0

# ─────────────────────────────────────────────────────────
#  OpenHands SDK (all 5 packages)
# ─────────────────────────────────────────────────────────
openhands>=1.0.0
openhands-sdk>=1.0.0
openhands-tools>=1.0.0
openhands-workspace>=1.0.0
openhands-agent-server>=1.0.0
claude-code-sdk>=0.0.1
